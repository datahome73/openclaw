{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'BASE64=\"eyJjaG...9fX0=\"; echo \"$BASE64\" | base64 -d > /tmp/openclaw.json && export OPENCLAW_CONFIG_PATH=/tmp/openclaw.json && exec node openclaw.mjs gateway --bind lan --allow-unconfigured'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
