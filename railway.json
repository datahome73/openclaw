{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "mkdir -p /home/node/.openclaw && cp /app/openclaw.json /home/node/.openclaw/openclaw.json && node openclaw.mjs gateway --bind lan",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
